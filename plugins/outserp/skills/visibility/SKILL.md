---
description: Report Outserp AI‑search visibility and competitor gaps. Use when the user asks how their brand shows up in ChatGPT/Claude/Gemini/Perplexity, who's beating them, or where the content gaps are.
---

# Outserp: visibility & competitor gaps

The user wants an AI‑visibility read from Outserp. Focus (if any): "$ARGUMENTS".

Use the Outserp MCP tools and synthesize a short, decision‑oriented summary:

1. `get_visibility_summary` — current visibility across AI engines + SERP.
2. `get_competitors` — share‑of‑voice / sentiment by engine and the top competitors.
3. `get_citations` — which of their pages and which domains get cited most.
4. `get_insights` and `get_opportunities` — what changed and what to do next.

Then recommend 2–3 concrete next actions (e.g. a comparison article targeting a gap,
a page to refresh). If the user wants to act, offer the `article` workflow or
`create_content_plan_grid`.
